import React from 'react';

const Hero = () => {
  return (
    <section className="hero">
      <h1>Find Your Next Web Development Job</h1>
      <p>Search thousands of web development jobs across the country.</p>
      <button>Browse Jobs</button>
    </section>
  );
}

export default Hero;
